import React, { useContext } from 'react';
import './Cart.css';

//Context
import { Context } from '../../Context';

const Cart = () => {
	//Carrito
	const { cart } = useContext(Context);

	return <></>;
};

export default Cart;
